const express = require("express"); const app = express(); app.get("/api/health", (req, res) => res.json({status: "OK", message: "Server is running!"})); app.listen(5000, () => console.log("✅ Server running on http://localhost:5000"));
